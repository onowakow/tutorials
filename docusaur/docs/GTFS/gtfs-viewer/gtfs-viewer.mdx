import { FileUpload } from './file-upload';
import { GTFSViewerStaging } from './gtfs-viewer-staging';

# GTFS Viewer

Now that we have a GTFS dataset, let's visualize the data.

<GTFSViewerStaging />
